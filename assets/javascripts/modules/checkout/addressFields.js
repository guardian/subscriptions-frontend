define([
], function (
) {
    'use strict';

    var postcode = function(required, label) {
    };

    var subdivision = function(required, label, values) {
    };

    return {
        postcode: postcode,
        subdivision: subdivision
    };
});
